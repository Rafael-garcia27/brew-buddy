Die Grundfläche: Karte mit 1-px-Kante; antippbar, wenn onClick gesetzt ist.

```jsx
<Card tone="accent">…</Card>
<Card onClick={pick} selected>…</Card>
```

Padding 16 px, Radius `--radius-card`. Hell/Dunkel ohne Schatten. Antippbar: scale(0.99) beim Drücken.
